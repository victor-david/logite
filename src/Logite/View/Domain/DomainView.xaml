<UserControl 
    x:Class="Restless.Logite.View.Domain.DomainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tk="http://schemas.restless.toolkit.com/2021"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:r="clr-namespace:Restless.Logite.Resources"
    xmlns:vw="clr-namespace:Restless.Logite.View.Domain"
    xmlns:vm="clr-namespace:Restless.Logite.ViewModel.Domain" d:DataContext="{d:DesignInstance Type=vm:DomainViewModel}"
    mc:Ignorable="d"
    d:DesignWidth="860" d:DesignHeight="400">

    <HeaderedContentControl Header="{Binding DisplayName, Mode=OneTime}" Tag="{Binding DomainStatus}">
        <Grid tk:Property.ColumnWidths="*,*,**" tk:Property.RowHeights="*,2*" Style="{StaticResource DataEntryPanelStyle}">
            <Grid.Resources>
                <Style TargetType="Border" BasedOn="{StaticResource SubContentBorderStyle}"/>
            </Grid.Resources>
            
            <Border>
                <vw:Method/>
            </Border>

            <Border Grid.Column="1">
                <vw:Status/>
            </Border>

            <Border Grid.Row="1" Grid.ColumnSpan="3">
                <vw:LogEntry/>
            </Border>

        </Grid>
    </HeaderedContentControl>
</UserControl>